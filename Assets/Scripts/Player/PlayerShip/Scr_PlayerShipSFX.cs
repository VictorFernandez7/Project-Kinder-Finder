using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlayerShipSFX : MonoBehaviour
{

    [Header("Sounds")]
    [SerializeField] private SoundDefinition explosion;
    [SerializeField] private SoundDefinition gates;
    [SerializeField] private SoundDefinition leaveAtmos;
    [SerializeField] private SoundDefinition hyperspace;
    [SerializeField] private SoundDefinition launchHyperspace;
    [SerializeField] private SoundDefinition postTakeOff;
    [SerializeField] private SoundDefinition startEngines;
    [SerializeField] private SoundDefinition thruster;
    [SerializeField] private SoundDefinition thrusterStop;
    [SerializeField] private SoundDefinition shutDown;

    // Update is called once per frame
    /*void Update()
    {
        if ()
        {
            Scr_MusicManager.Instance.PlayRandom(explosion.Sound);
        }
        if ()
        {
            Scr_MusicManager.Instance.PlaySound(gates.Sound, 0);
        }
        if ()
        {
            Scr_MusicManager.Instance.PlaySound(leaveAtmos.Sound, 0);
        }
        if ()
        {
            Scr_MusicManager.Instance.PlaySound(hyperspace.Sound, 0);
        }
        if ()
        {
            Scr_MusicManager.Instance.PlaySound(launchHyperspace.Sound, 0);
        }
        if ()
        {
            Scr_MusicManager.Instance.PlaySound(postTakeOff.Sound, 0);
        }
       
        if ()
        {
            Scr_MusicManager.Instance.PlaySound(shutDown.Sound, 1);
        }
        if ()
        {
            Scr_MusicManager.Instance.PlaySound(startEngines.Sound, 0);
        }
        if ()
        {
            Scr_MusicManager.Instance.PlaySound(thruster.Sound, 1);
        }
        if ()
        {
            Scr_MusicManager.Instance.PlaySound(thrusterStop.Sound, 0);
        }
    }
    */
    
}
