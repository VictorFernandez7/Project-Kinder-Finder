using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Scr_UISFX : MonoBehaviour
{
    [Header("Sounds")]
    [SerializeField] private SoundDefinition button;
    [SerializeField] private SoundDefinition buttonLight;
    [SerializeField] private SoundDefinition addTool;
    [SerializeField] private SoundDefinition animateIn;
    [SerializeField] private SoundDefinition animateOut;
    [SerializeField] private SoundDefinition exitGame;
    [SerializeField] private SoundDefinition loadGame;
    [SerializeField] private SoundDefinition newGame;
    [SerializeField] private SoundDefinition pause;
    [SerializeField] private SoundDefinition resume;
    [SerializeField] private SoundDefinition shipUpgrade;

    // Update is called once per frame
    /*void Update()
    {
        if ()
        {
            Scr_MusicManager.Instance.PlaySound(button.Sound, 0);
        }
        if ()
        {
            Scr_MusicManager.Instance.PlaySound(button.Sound, 1);
        }
        if ()
        {
            Scr_MusicManager.Instance.PlaySound(button.Sound, 2);
        }
        if ()
        {
            Scr_MusicManager.Instance.PlaySound(buttonLight.Sound, 0);
        }
        if ()
        {
            Scr_MusicManager.Instance.PlaySound(buttonLight.Sound, 1);
        }
        if ()
        {
            Scr_MusicManager.Instance.PlaySound(buttonLight.Sound, 2);
        }
        if ()
        {
            Scr_MusicManager.Instance.PlayRandom(animateIn.Sound);
        }
        if ()
        {
            Scr_MusicManager.Instance.PlayRandom(animateOut.Sound);
        }
        if ()
        {
            Scr_MusicManager.Instance.PlaySound(loadGame.Sound, 0);
        }
        if ()
        {
            Scr_MusicManager.Instance.PlayRandom(exitGame.Sound);
        }
        if ()
        {
            Scr_MusicManager.Instance.PlayRandom(newGame.Sound);
        }
        if ()
        {
            Scr_MusicManager.Instance.PlayRandom(pause.Sound);
        }
        if ()
        {
            Scr_MusicManager.Instance.PlayRandom(resume.Sound);
        }
        if ()
        {
            Scr_MusicManager.Instance.PlayRandom(shipUpgrade.Sound);
        }
        */
    }

