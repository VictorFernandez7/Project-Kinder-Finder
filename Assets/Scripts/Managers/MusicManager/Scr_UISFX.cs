using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Scr_UISFX : MonoBehaviour
{
    [Header("Sounds")]
    [SerializeField] private SoundDefinition button;
    [SerializeField] private SoundDefinition buttonLight;

    // Update is called once per frame
    void Update()
    {
        
    }
}
